# Better Auth Configuration
# The base URL of your application (e.g., http://localhost:3000)
BETTER_AUTH_URL=""
# A secret key for signing session tokens. Generate one using `openssl rand -base64 32`
BETTER_AUTH_SECRET=""

# Google OAuth (for Sign In)
# Obtain these from Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Google Service Account (for Sheet Access)
# Create a Service Account in Google Cloud Console, enable Google Sheets API, and download the JSON key.
GOOGLE_SERVICE_ACCOUNT_EMAIL=""
GOOGLE_PRIVATE_KEY=""

# Google Sheet Configuration
# The ID of your Google Sheet. Found in the URL: https://docs.google.com/spreadsheets/d/[YOUR_SHEET_ID]/edit
# Make sure to share the sheet with the Service Account Email (Editor access).
GOOGLE_SHEET_ID=""
